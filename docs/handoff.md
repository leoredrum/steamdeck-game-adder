# 当前进度 (handoff)

> 每次收工时最后一位 AI 更新本文件。接棒 AI 先读这个。

**最近更新**: 2026-04-26 by Claude

---

## ✅ 已完成

- 项目初始化，scaffold 起���
- GTK3 拖拽界面（两个拖拽区：EXE + 封面图）
- 二进制 VDF 解析器（读写 shortcuts.vdf）
- 自动识别中文游戏名（从 Game/ 下一级目录提取，过滤日文/英文/版���后缀）
- 自动设置 Proton 兼容层（写入 config.vdf CompatToolMapping）
- 封面图自动安装到 Steam grid 目录
- 重复添加自动覆盖（不弹窗）
- 清理无效游戏按���（检测 exe 不存在的，勾选后删除文件夹 + 从 Steam 移除）

## 🚧 正在做

(无，等待用户反馈)

## 🔜 下一步

- 收集用户实际使用反馈
- 根据反馈优化游戏名识别逻辑
- 考虑是否需要批量添加功能

## ⚠️ 已知坑

- Steam Deck 文件系统只读，不能用 pip 安装第三方库，只能用系统自带的 Python 标准库 + GTK3
- tkinter 在 Linux 上不支持原生拖拽，所以改用 GTK3
- Edit 工具对中文字符匹配有时会因编���问题失败，需要用 Write 整体重写

## ❓ 待用户决策

- 是否需要批量添加多个游戏？
- 是否需要接入 SteamGridDB 自动搜索封面？

---

## 接棒说明

你好，我是接班的 AI。按 `AGENTS.md` 的协议:
1. 读过 `docs/spec.md` 了吗?
2. 读过 `README.md` 了吗?
3. 跟用户打个招呼，一句话复述你理解的"现状 + 下一步"，让用户确认
4. 开工。每完成一个工作单元，更新本文件 + commit + push
